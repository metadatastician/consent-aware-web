// SPDX-License-Identifier: CC-BY-SA-4.0
// Copyright (c) Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
// SPDX-FileCopyrightText: 2024-2026 Jonathan D.A. Jewell (hyperpolymath)
= Consent-Aware Web
:toc:
:toc-placement: preamble

// ── Licensing ───────────────────────────────────────────────────────────────
image:https://img.shields.io/badge/Code-MPL--2.0-blue.svg?logo=mozilla[Code licence: MPL-2.0,link="https://opensource.org/licenses/MPL-2.0"]
image:https://img.shields.io/badge/Docs-CC--BY--SA--4.0-blue.svg?logo=creativecommons[Docs licence: CC-BY-SA-4.0,link="https://creativecommons.org/licenses/by-sa/4.0/"]
image:https://img.shields.io/badge/Provenance-Quantum--Safe-blueviolet[Quantum-Safe Provenance,link="docs/legal/EXHIBIT-B-QUANTUM-SAFE.txt"]

// ── Standard & quality gates ────────────────────────────────────────────────
image:https://img.shields.io/badge/RSR-Rhodium_Standard-9C27B0[Rhodium Standard Repository,link="https://github.com/hyperpolymath/rhodium-standard-repositories"]
image:https://api.scorecard.dev/projects/github.com/metadatastician/consent-aware-web/badge[OpenSSF Scorecard,link="https://scorecard.dev/viewer/?uri=github.com/metadatastician/consent-aware-web"]
image:https://sonarcloud.io/api/project_badges/measure?project=metadatastician_consent-aware-web&metric=alert_status[SonarQube Quality Gate,link="https://sonarcloud.io/summary/new_code?id=metadatastician_consent-aware-web"]
image:https://archive.softwareheritage.org/badge/origin/https://github.com/metadatastician/consent-aware-web/[Archived in Software Heritage,link="https://archive.softwareheritage.org/browse/origin/?origin_url=https://github.com/metadatastician/consent-aware-web"]

// ── Provenance & ecosystem ──────────────────────────────────────────────────
image:https://api.thegreenwebfoundation.org/greencheckimage/github.com[Green Web,link="https://www.thegreenwebfoundation.org/green-web-check/?url=github.com"]

IETF-track specifications for consent-aware HTTP: AIBDP manifests and the 430 Consent Required status code

== Overview

Consent-Aware Web is a specification suite for ethical AI governance on the web:
a way for content originators to declare what AI systems may and may not do with
their content, and for origins to enforce those declarations at the HTTP layer.

It began as a `robots.txt`-shaped idea and grew into a consent model — because
`robots.txt` governs *crawling*, and the question that matters now is *use*.

== Problem

The web has no standard way to express, or enforce, any of the following:

* machine-readable AI usage boundaries
* transparent identification of AI agents
* enforceable consent
* verifiable content provenance
* standardised compliance reporting

== Architecture — and what is actually specified

The full framing is six layers. *Two are specified today.* The remaining four are
intended, not drafted, and are listed here as direction rather than deliverable.

[cols="1,2,3,1",options="header"]
|===
| Layer | Protocol | Purpose | Status

| Declaration
| AI Boundary Declaration Protocol (AIBDP)
| Declares permitted and prohibited AI uses of content
| *`draft-jewell-aibdp-00`*

| Enforcement
| HTTP Status Code 430 (Consent Required)
| Runtime enforcement of consent requirements
| *`draft-jewell-http-430-consent-required-00`*

| Identity
| AI Agent Identification Protocol
| AI systems declare who they are and what they do
| not drafted

| Consent Flow
| Web Consent Management Protocol
| How consent is requested, granted and tokenised
| not drafted

| Provenance
| Content Provenance Protocol
| Origin and AI involvement in content
| not drafted

| Accountability
| AI Compliance Reporting Framework
| Monitoring, audit and violation reporting
| not drafted
|===

Both drafts are `-00` and have *not* been submitted to the IETF datatracker. See
`.machine_readable/descriptiles/STATE.a2ml` for the current position and blockers.

== How it works

An origin publishes a manifest at `/.well-known/aibdp.json` declaring its
policies. When a request arrives whose declared or inferred AI purpose violates
one of those policies, the origin answers *430 Consent Required* rather than 200
or 403 — a distinct code, because the refusal is procedural (consent was never
established) rather than authorisation (403) or legal (451).

This repository publishes its own manifest at `.well-known/aibdp.json`. That is
not decoration: the `dogfood-gate` and `schema-validate` jobs fail if it is
missing or invalid.

== Quick start

[source,bash]
----
just test                   # validate drafts, manifests, links, expiry
just build-drafts           # render drafts/*.xml

cd examples/reference-implementations/bun && bun run example_server.js
curl -o /dev/null -w '%{http_code}\n' localhost:5000/article -H 'User-Agent: GPTBot/1.0'   # 430
----

== Repository layout

[cols="1,3"]
|===
| Path | Contents

| `drafts/` | The Internet-Drafts. Single source of truth — see ADR-0005; no `draft-*.xml` may exist at the root.
| `schemas/` | `aibdp-schema-v0.2.json` (JSON Schema draft-2020-12)
| `.well-known/aibdp.json` | This repository's own AIBDP declaration
| `examples/reference-implementations/` | Runtime-neutral middleware; Bun (tier 1) and Deno (tier 2) servers
| `docs/` | Explainer, FAQ, conformance, ethics, server configurations, ADRs
| `src/*.affine` | AffineScript sources
|===

== What you get

* *The specifications* — two Internet-Drafts in `drafts/`, RFC-XML v3, plus the
  AIBDP JSON Schema in `schemas/`.
* *A working reference implementation* — `examples/reference-implementations/`,
  one runtime-neutral core with Bun and Deno entry points. Enforcement is
  verified by making requests and asserting 430, not by inspecting files.
* *This repository's own AIBDP declaration* — `.well-known/aibdp.json`,
  validated in CI. The dogfooding claim is checked, not asserted.
* *Machine-readable metadata* (`.machine_readable/descriptiles/`) — `STATE`,
  `META`, `ECOSYSTEM`, `PLAYBOOK`, `AGENTIC`, `NEUROSYM`, `CLADE` and `ANCHOR`
  in a2ml, so tools and agents can read the project's state and boundaries.
* *AI gatekeeper protocol* — `0-AI-MANIFEST.a2ml` is the entry point that tells
  an agent how to work here before it touches anything.
* *Gates that can fail* — see below. Every one of them corresponds to a defect
  that was actually present in this tree before extraction.
* *Governance* — `GOVERNANCE.adoc`, `MAINTAINERS.adoc`, `.github/` community
  health files, and a release `AUDIT.adoc` gate.

== The gates

[cols="1,3"]
|===
| Job | What it fails on

| `single-source` | Any `draft-*.xml` at the repository root, or two drafts sharing a base name. The 430 draft once existed twice with different normative text — see ADR-0005.
| `rfcxml-v3` | A draft missing `version="3"`, `seriesInfo`, `submissionType` or `expiresDate`.
| `no-v2` | RFC-XML v2 constructs (`<section title=>`, `<list>`, `hangText=`, an rfc2629 DOCTYPE). The AIBDP draft was v2 throughout while declaring v3.
| `schema-validate` | Any manifest that does not validate against `schemas/aibdp-schema-v0.2.json`, including this repo's own.
| `claims-audit` | Any repository path a governance document asserts but which is not on disk. The predecessor claimed RSR Gold against files that did not exist.
| `draft-expiry` | A draft past its `expiresDate`. Red here is the gate working.
|===

== Repository map

[cols="1,3"]
|===
| Path | What lives there

| `drafts/`                 | The Internet-Drafts. Single source of truth.
| `schemas/`                | AIBDP JSON Schema (draft-2020-12).
| `.well-known/aibdp.json`  | This repository's own AIBDP declaration.
| `examples/`               | Reference implementations and worked manifest scenarios.
| `src/`                    | AffineScript sources.
| `0-AI-MANIFEST.a2ml`      | Universal entry point for AI agents (read first).
| `.machine_readable/`      | Project metadata, policies, contractiles, AI configs.
| `docs/`                   | Explainer, FAQ, conformance, ethics, onboarding, decisions.
| `build/`, `Justfile`      | Task orchestration (`just` phases delegate to `build/just/`).
| `scripts/`                | The gate implementations.
| `.github/`                | Workflows + community health files.
|===

== Where to go next

* `EXPLAINME.adoc` — the engineering deep-dive: how the pieces actually work.
* `AFFIRMATION.adoc` — the dated, signed honesty snapshot of the repo's true state.
* `AUDIT.adoc` — the release audit gate.

== Licence

Code, configuration and scripts are link:LICENSE[Mozilla Public License 2.0]
(`MPL-2.0`); prose documentation is `CC-BY-SA-4.0`. Both texts live in
`LICENSES/`, and per-file `SPDX-License-Identifier` headers are authoritative.
The GitHub-detected licence is MPL-2.0 (the root `LICENSE`). Long-term
attribution uses Quantum-Safe Provenance — see
link:docs/legal/EXHIBIT-B-QUANTUM-SAFE.txt[the Quantum-Safe Provenance exhibit].
