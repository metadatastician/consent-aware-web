// SPDX-License-Identifier: MIT OR GPL-3.0-or-later
// Node.js bindings. AffineScript port of Node.res.

module Node;

module Fs {
  extern fn read_file(path: String, enc: String) -> Promise<String> = "fs/promises" "readFile";
}

module Path {
  extern fn join(a: String, b: String) -> String = "path" "join";
}

extern fn date_now() -> Float = "Date" "now";
